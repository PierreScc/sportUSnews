<h1>Articles Sport US</h1>

<% @articles.each do |article| %>
  <div>
    <h2><%= link_to article.title, article_path(article) %></h2>
    <p><%= article.league %> - <%= truncate(article.content, length: 100) %></p>
  </div>
<% end %>

<%= link_to 'Créer un nouvel article', new_article_path %>
