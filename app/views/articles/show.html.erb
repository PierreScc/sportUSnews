<h2>Commentaires :</h2>
<%= form_with(model: [ @article, Comment.new ]) do |form| %>
  <p>
    <%= form.text_area :content %>
  </p>
  <p>
    <%= form.submit "Poster un commentaire" %>
  </p>
<% end %>

<% @article.comments.each do |comment| %>
  <p><%= comment.content %></p>
<% end %>
